# FastAPI Configuration
PORT=8080
HOST=0.0.0.0

# Model Configuration - MLflow Registry (Primary)
MLFLOW_TRACKING_URI=http://127.0.0.1:5000
MODEL_NAME=archive-forecast-rf-v1
MODEL_STAGE=Production

# Model Configuration - Disk Path (Fallback)
MODEL_PATH=ml-poc/models/model.joblib

# MLflow Experiment Configuration
MLFLOW_EXPERIMENT_NAME=archive-forecast
